version: "2"

linters:
  default: standard
  enable:
    - gosec
    - misspell
    - revive
    - unconvert
    - copyloopvar
  settings:
    revive:
      rules:
        - name: exported
          disabled: false

formatters:
  enable:
    - gofmt
    - goimports
